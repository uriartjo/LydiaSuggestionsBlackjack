package com.company;

import com.studiohartman.jamepad.ControllerManager;

public class Controller {
    public static ControllerManager controllers = new ControllerManager(); //This will make it MUCH easier to use the controller everywhere else. You'll see in some of the methods that use the controller what I mean.

}
